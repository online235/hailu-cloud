<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hailu.cloud.api.mall.module.goods.dao.IconMapper">

    <select id="iconList" parameterType="Integer"
            resultType="com.hailu.cloud.api.mall.module.goods.vo.IconVo">
		SELECT 
		id,
		name,
		path AS iconPath,
		type 
		FROM t_s_icon 
		WHERE type=#{type}
	</select>
</mapper>